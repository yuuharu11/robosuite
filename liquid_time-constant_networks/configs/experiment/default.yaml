# @package _global_
defaults:
  - /pipeline: default
  - /model: default

# fundamental settings
model:
  dropout: 0.0
  n_layers: 4
  d_model: 256
  prenorm: true
  norm: batch

loader:
  batch_size: 2000

optimizer:
  lr: 0.001
  weight_decay: 0.0

trainer:
  max_epochs: 5